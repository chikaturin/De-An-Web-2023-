@model ShopDunk.Models.Customer

@{
    ViewBag.Title = "FromLogin";
    Layout = "~/Views/Shared/MasterLayout.cshtml";

}
<head>
    <link rel="stylesheet" href="~/Content/Login.css" />
</head>
@using (Html.BeginForm("LoginAccountCus", "LoginUser"))
{
    @Html.AntiForgeryToken()
    <div class="main container d-flex justify-content-center mt-4 mb-4">
        <div class="row d-flex justify-content-center">
            <div class="col-6"><img style="width:100%;height:500px;" src="https://shopdunk.com/images/uploaded/banner/VNU_M492_08%201.jpeg" /></div>
            <div class="col-6">
                <div class="dangnhap">
                    <p>Tên đăng nhập:</p>
                    @Html.TextBoxFor(model => model.CusPhone, new { style = "field-validation-error", maxlength = 10 })
                    @Html.ValidationMessageFor(model => model.CusPhone, "", new { @class = "text-danger" })
                </div>
                <div class="matkhau">
                    <p>Mật khẩu:</p>
                    @Html.PasswordFor(model => model.PasswordConfirm, new { @type = "password", maxlength = 20 })
                    @Html.ValidationMessageFor(model => model.PasswordConfirm, "", new { @class = "text-danger" })
                    @if (ViewBag.ErrorInfo == null)
                    {
                        <p>@ViewBag.ErrorInfo</p>
                    }
                </div>
                <br />
                <div class="nhomatkhau">
                    <input type="checkbox" class="check" /><span style="margin-left: 3px;">Nhớ mật khẩu</span>
                    <br />
                    <input type="submit" value="Đăng nhập" class="button_dk" id="button_dk" />
                    <div class="chuacotk d-flex row mt-3">
                        <a class="col-4" href="@Url.Action("Index", "LoginUser")"><p>Đăng nhập cho Admin</p></a>
                        <span> Bạn Chưa Có Tài Khoản? </span>
                        <a href="@Url.Action("RegisterCus", "LoginUser")"><p>Tạo tài khoản ngay</p></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
}


